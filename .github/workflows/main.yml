name: helloworlds

on:
    workflow_dispatch:

jobs:
    ci:
        run on: self-hosted
        step:
            -name: run helloworlds
             run: echo 'Hello World'
             
        
        